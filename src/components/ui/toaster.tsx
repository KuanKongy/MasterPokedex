
import React from 'react';
import { useToast } from '@chakra-ui/react';

export function Toaster() {
  // This component doesn't need to do anything with Chakra UI
  // since Chakra toasts are rendered via the useToast hook directly
  return null;
}
